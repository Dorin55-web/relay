# ctranslate2 is imported directly by translator.py and cuda_setup.py, but is
# not pinned here on purpose: it arrives with faster-whisper, and that pin is
# exact, so the two can never disagree about which build is installed.
faster-whisper==1.2.1
nvidia-cublas-cu12
nvidia-cudnn-cu12>=9.0
sounddevice>=0.5.5
pynput>=1.8.1
pyperclip>=1.11.0
numpy>=2.0
scipy>=1.11
PySide6-Essentials>=6.7
comtypes>=1.4
sentencepiece>=0.2      # tokenizer for the typed-text translator
